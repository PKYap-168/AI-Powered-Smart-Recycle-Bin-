# Training Configuration

This folder contains the configuration files used during YOLO model training and dataset setup.

- `data.yaml` — defines the dataset paths and class names used for training.
- `args.yaml` — contains the training parameters and settings recorded for the final experiment.

These files are included to improve reproducibility of the training process.
