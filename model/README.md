# Final Selected Model

The final model selected for deployment was **YOLOv8s** trained using the **light augmentation** configuration.

This model was selected after baseline parameter tuning, augmentation evaluation, candidate model comparison, and external-image testing.

The trained weights are stored as:

```text
best.pt
